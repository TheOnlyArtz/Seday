const guildOrInit = require('./guildExists.js');
const initGuild = require('./initGuild.js');
const updator = require('./updator.js');

module.exports = {
    guildOrInit,
    initGuild,
    updator
}